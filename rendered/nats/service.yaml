apiVersion: v1
kind: Service
metadata:
  name: nats
  namespace: nats
spec:
  type: NodePort
  selector:
    app: nats
  ports:
  - name: client
    port: 4222
    targetPort: 4222
    nodePort: 30422

