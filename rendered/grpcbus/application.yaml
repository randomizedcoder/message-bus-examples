apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grpcbus
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/randomizedcoder/message-bus-examples.git
    targetRevision: main
    path: rendered/grpcbus
    directory:
      exclude: 'application.yaml'
  destination:
    server: https://kubernetes.default.svc
    namespace: grpcbus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

